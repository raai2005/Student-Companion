// JSON formatting for flashcards/quizzes
export function formatQuizData(data: any) {
  // Format quiz data
  return data;
}

export function formatFlashcardData(data: any) {
  // Format flashcard data
  return data;
}
